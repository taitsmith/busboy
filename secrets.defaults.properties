# Committed fallback for the secrets-gradle-plugin.
# Real values belong in secrets.properties (gitignored), which overrides every
# key here. This file exists only so a fresh clone or CI can configure the build
# without the real keys; network calls will fail at runtime with these values.
ac_transit_key=PLACEHOLDER
cta_key=PLACEHOLDER
google_maps_key=PLACEHOLDER
google_directions_key=PLACEHOLDER
MAPS_API_KEY=PLACEHOLDER
